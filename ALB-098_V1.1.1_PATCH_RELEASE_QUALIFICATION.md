# ALB-098 — AlbumAI Pro 1.1.1 Patch Release Qualification

Status: **RELEASE QUALIFIED — AUTOMATED, CCX, AND INSTALLED RUNTIME PASS**

## Root cause

The published immutable `v1.1.0` tag points to
`fa5cc59adcb49807e680ea544bd5c81c68d956d8`. Direct CCX qualification and the
canonical panel version fix were merged later in `f22909ae146a02e6ac914708556e2d5dbaee8f5c`.
Attaching that later CCX to the older release would create a false source and
artifact provenance claim.

## Minimal patch boundary

- Version: `1.1.1`
- Runtime identity: `ALB-098-v1.1.1-patch-v1`
- Plugin ID: `com.albumai.pro`
- Host: Photoshop only
- Manifest: v5, no network permission
- Source behavior: unchanged except canonical visible version identity
- Historical `v1.1.0` tag, assets, checksums, and qualification: immutable

## Automated gate

Run from `com.albumai.pro/`:

```text
npm run verify:ci
```

The gate must pass deterministic tests through ALB-098, architecture,
regression, hardening, production build and bundle identity, reproducible ZIP
packaging, direct-distribution preflight, dependency audits, dependency graph,
and committed `dist` reproducibility.

Automated result:

- Complete qualification command: **PASS**
- Production bundle: `716659` bytes
- Production bundle SHA-256:
  `62a2fc71bc402b9895d60207cb7b587b3eee0a01fb8ae5abf9fc5e414b635fc8`
- Reproducible ZIP: `AlbumAI-Pro-1.1.1.zip`, `192736` bytes
- ZIP SHA-256:
  `2cfe0237d468ed3a140b4fab725887ca4ab7f06df2f48d247d1d4dba24548ee9`
- Direct-distribution preflight: **PASS**
- Committed `dist` cleanliness: pending the approval-gated commit

## CCX gate

1. Build the final production `dist` from the reviewed patch commit.
2. In UXP Developer Tool, load only `dist/manifest.json` and use **Package**.
3. Save `com.albumai.pro_PS.ccx` outside the repository.
4. Verify it with:

```text
npm run distribution:verify -- --ccx /absolute/path/to/com.albumai.pro_PS.ccx
```

5. Record CCX size, SHA-256, embedded version, build ID, and runtime checksum.

CCX result:

- Qualified artifact: `com.albumai.pro_PS.ccx`
- Artifact size: `188473` bytes
- Artifact SHA-256:
  `ec50eed854563ee445fec4772b6400a17e53211bf55a4cb6c1b02f6107b2cd3d`
- Embedded version: `1.1.1`
- Embedded file count: `8`
- Embedded runtime identity: `ALB-098-v1.1.1-patch-v1`
- Embedded runtime SHA-256:
  `62a2fc71bc402b9895d60207cb7b587b3eee0a01fb8ae5abf9fc5e414b635fc8`
- CCX verification: **PASS**

## Installed runtime gate

1. Unload the UDT development plugin and close Photoshop.
2. Install the verified CCX through Creative Cloud Desktop.
3. Restart Photoshop and open AlbumAI Pro from the Plugins menu.
4. Confirm both visible badges show `v1.1.1`.
5. Open the existing REC005 multi-template project.
6. Re-authorize the photo folder if requested, then confirm six photos, four
   sheets, A-B-A-B template mapping, two registered templates, and persisted
   slot assignments.

A full render rerun is not required because ALB-098 does not alter placement,
Photoshop execution, persistence, or output behavior.

Installed runtime result:

- Creative Cloud Desktop installation: **PASS**
- Photoshop startup from the installed CCX: **PASS**
- Header and welcome badges: `v1.1.1`
- Existing REC005 project reopen: **PASS**
- Photo-folder re-authorization and six-photo library restore: **PASS**
- Four-sheet multi-template storyboard and persisted assignments: **PASS**
- Registered templates: `2`; ready: `2`; blocking: `0`
- Recovery compatibility: **Compatible**
- Full render rerun: not required for this identity/provenance-only patch

## Release gate

Do not tag, publish, overwrite release assets, or commit/push without explicit
approval. After all gates pass, the `v1.1.1` tag must point to the exact reviewed
commit used to produce both release artifacts.
